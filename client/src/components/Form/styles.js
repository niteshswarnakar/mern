import { makeStyles } from "@mui/styles";

export default makeStyles(() => {
  return {
    container: {
      height: "60vh",
      padding: "10px",
    },
    formcontainer: {
      display: "flex",
      flexDirection: "column",
      gap: "20px",
    },
  };
});
